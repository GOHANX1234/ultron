<?xml version="1.0" encoding="utf-8"?>
<!-- The window behind the process start. Only a flat colour: the animated
     splash is SplashScreen in Dart, and this exists purely so the frames
     before the engine draws are the same colour it starts from. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background" />
</layer-list>
